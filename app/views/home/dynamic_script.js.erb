var number1 = window.prompt("Type the first number");
var number2 = window.prompt("Type the second number");
var result = parseInt(number1) + parseInt(number2);
alert("The result is " + result + ".\n\nJust for fun, grab a random number from the server: <%= rand(1..100) %>");

//# sourceURL=dynamic_script.js